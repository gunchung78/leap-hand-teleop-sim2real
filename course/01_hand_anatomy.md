# 1단원 — 로봇 손 이해: 16개 모터, 두 가지 이름, 한 가지 규약

> 원문: `leap_hand_mapping/joint_map.py`, `README.md` Phase 0, `docs/real_hand_bringup.md`

## 목표

- LEAP Hand v1 의 모터 16개가 **어디에 붙어 있고 무엇을 움직이는지** 손으로 가리킬 수 있다.
- 같은 관절을 부르는 두 이름 체계(MuJoCo 이름 / 실기 모터 ID)를 서로 바꿔 쓸 수 있다.
- 이 저장소의 모든 벡터가 **어느 순서·어느 단위·어느 0점**인지 안다 — 이후 단원의 전제다.

## 배경

LEAP Hand 는 손가락 4개(검지·중지·약지·엄지, **새끼손가락 없음**) × 모터 4개 = 16 자유도다. 모터는 Dynamixel
XL330-M288, 데이지체인 한 줄로 U2D2 에 물린다. 우리 것은 **Lite** — 기어가 플라스틱이라 전류 한계 350 mA 를 넘기면
이빨이 깨진다(3단원).

### 손가락 하나의 구조 (손바닥 → 손끝)

```
손바닥 ── [벌림 rot] ── [굽힘 mcp] ── [중간 pip] ── [끝 dip] ── 손끝
          ↑ 한 덩어리(같은 하우징) ↑
```

앞의 두 모터는 한 덩어리에 들어 있어 헷갈리기 쉽다. **벌림(rot)** 은 손가락을 옆으로 벌리고, **굽힘(mcp)** 은
손가락 전체를 손바닥 쪽으로 접는다. 엄지는 뿌리 회전(cmc) → 축 회전(axl) → 굽힘(mcp) → 끝 굽힘(ipl).

### 두 이름 체계

| 손가락 | 위치 | MuJoCo 이름 | 모터 ID | 움직임 |
|---|---|---|---|---|
| 검지 | 뿌리 | `if_rot` | **0** | 벌림 (+ 가 중지 쪽) |
| | 뿌리 | `if_mcp` | **1** | 굽힘 |
| | 중간 | `if_pip` | 2 | |
| | 끝 | `if_dip` | 3 | |
| 중지 | 뿌리 | `mf_rot` | **4** | 벌림 (+ 약지 쪽, − 검지 쪽) |
| | 뿌리 | `mf_mcp` | **5** | 굽힘 |
| | 중간 | `mf_pip` | 6 | |
| | 끝 | `mf_dip` | 7 | |
| 약지 | 뿌리 | `rf_rot` | **8** | 벌림 (− 가 중지 쪽) |
| | 뿌리 | `rf_mcp` | **9** | 굽힘 |
| | 중간 | `rf_pip` | 10 | |
| | 끝 | `rf_dip` | 11 | |
| 엄지 | 뿌리 | `th_cmc` | 12 | 손바닥 가로지르는 회전 |
| | | `th_axl` | 13 | 축 회전 (마주보기) |
| | | `th_mcp` | 14 | 굽힘 |
| | 끝 | `th_ipl` | 15 | 끝 굽힘 |

굵은 숫자를 보라: **손가락마다 벌림·굽힘의 순서가 두 체계에서 반대**다. 이게 2단원의 주제다.

### 이 저장소의 규약 (외울 것 세 줄)

1. **순서**: 코드 안의 16차원 벡터는 특별한 말이 없으면 **MuJoCo 순서** (`MUJOCO_JOINT_NAMES`). 실기 모터 순서로 바꾸는
   건 `joint_map` 이 한다.
2. **단위**: 라디안. 로그에만 도(deg)를 쓴다.
3. **0점**: 편 손 = 0 (MuJoCo). 실기는 편 손 = π(모터 180°). 변환은 `mujoco_to_leaphand()`.

## 실습

```bash
conda activate leap-hand
python -m leap_hand_mapping.joint_map
```

```
MuJoCo 관절이름  손가락  모터ID       MJCF 범위          실기 범위           교집합            텔레옵
  0 if_mcp   검지     1  [-0.314,+2.230]  [-0.314,+2.230]  [-0.314,+2.230]  [-0.314,+2.230]
  1 if_rot   검지     0  [-1.047,+1.047]  [-1.047,+1.047]  [-1.047,+1.047]  [-1.047,+0.052]
  ...
self_check 통과
```

그리고 파이썬에서 직접:

```python
from leap_hand_mapping import joint_map as jm
import numpy as np
q = np.zeros(16); q[jm.MUJOCO_JOINT_NAMES.index("if_mcp")] = 0.5   # 검지 굽힘 0.5 rad
print(jm.mujoco_to_leaphand(q).round(3))     # 실기 명령: 모터 1 이 π+0.5, 나머지 π
print(jm.MUJOCO_TO_MOTOR)                    # [1 0 2 3 5 4 6 7 9 8 10 11 12 13 14 15]
```

시뮬로 눈에 익히기(실기 없이):

```bash
python scripts/phase0/p0_3_sweep_joints.py          # MuJoCo 뷰어에서 관절을 하나씩 왕복
```

## 관찰할 것

- `mujoco_to_leaphand(q)` 결과에서 **0.5 가 들어간 자리가 인덱스 1** 이다(모터 ID 1). 이름은 `if_mcp` 인데 자리는 두 번째.
- 범위 열 네 개가 다 같지 않다. `th_axl`/`th_mcp` 는 두 모델이 다르고(2단원), `*_rot` 은 텔레옵 열이 훨씬 좁다(8단원 사례 13).
- `p0_3` 에서 벌림 모터(0/4/8)가 움직일 때 옆 손가락과의 간격을 보라. 편 손에서는 ±7.7° 만 벌려도 닿는다(README Phase 0).

## 문제

1. 약지를 중지 쪽으로 붙이려면 어느 이름의 관절을 어느 부호로 움직여야 하나? 검지는?
2. 실기에서 모터 ID 5 가 뜨겁다. MuJoCo 이름으로 어느 관절이고, 어떤 동작이 그 모터에 부하를 주는가?
3. `q_real = np.full(16, np.pi)` 를 실기에 보내면 손은 어떤 자세인가? `np.zeros(16)` 를 보내면?
4. LEAP 손에 새끼손가락이 없다는 사실이 텔레오퍼레이션(사람 손 → 로봇 손)에서 무엇을 뜻하는가?

## 해설

1. 약지: `rf_rot` 을 **−** 방향(모델에서 확인: −0.3 rad 에 약지–중지 손끝 거리 45→32 mm). 검지: `if_rot` 을 **+** 방향.
   중지는 `mf_rot` + 가 약지 쪽. 부호는 추측하지 말고 항상 모델로 확인한다(8단원 사례 13 에서 실제로 그렇게 했다).
2. 모터 5 = `mf_mcp`, 중지 굽힘. 주먹을 쥔 채 무언가를 누르거나, 관절 한계·물체에 막힌 채 목표를 유지할 때 스톨 전류가 흐른다.
3. 전부 π = **편 손**(실기 0점). 전부 0 = 모터 0° = 편 손에서 180° 돌아간 자세로, 관절 범위 밖이라 클립되거나 스톨한다.
   그래서 실기로 나가는 길은 `safe_leaphand_command`(클립 → 치환 → +π) 하나뿐이다.
4. 사람의 새끼손가락 랜드마크는 버린다. 리타겟팅은 검지·중지·약지·엄지 4개만 대응시키며, 사람의 약지·새끼 동작이
   로봇 약지 하나에 어떻게 반영되는지는 설계 선택이다(5단원).

## 다음 단원

2단원 — 관절 매핑 함정: 위 표의 "순서가 반대"를 순기구학으로 증명한다.
